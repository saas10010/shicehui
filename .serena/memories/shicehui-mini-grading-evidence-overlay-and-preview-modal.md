# Mini 批改确认页：对/错画在图片上 + 大图预览

## 用户澄清
- 需要的是“在作业图片里直接打对勾/打叉”，而不是在图片旁边/组件里显示对错。

## 实现（原型）
- `components/mini/teacher/grading-confirm-panel.tsx`
  - 缩略图：在图片上用 SVG 线条覆盖绘制 ✓/✗（无额外文字标签，aria-hidden）。
  - 点击缩略图：弹出页面内“大图预览”弹层，同样在大图上覆盖绘制 ✓/✗。
  - 目前标注点为示例（基于 activeStudentId + 页索引生成），真实系统应由识别定位结果驱动。
