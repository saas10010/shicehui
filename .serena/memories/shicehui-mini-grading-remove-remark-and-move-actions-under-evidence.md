# Mini 批改确认页：去掉备注输入，操作按钮移到证据说明下方

## 用户需求
- 去掉「备注/订正点（可选）」输入区。
- 将「重新识别」「确认并下一位」放到证据区域内，紧跟在“左右滑动/上一张下一张说明”下面，减少页面分块与滚动。

## 实现
- `components/mini/teacher/grading-confirm-panel.tsx`
  - 移除备注 textarea 及其相关本地状态/缓存逻辑。
  - 将「重新识别」「确认并下一位」按钮移动到证据卡片底部（说明文案下方）。
  - 已确认时继续保留「撤销确认」按钮（同区域）。
